export var convertToPx = function convertToPx(size) {
  var px = size * 16;
  return px;
};
export var rotateDegree = function rotateDegree(count) {
  var rotate = 360 / count;
  return rotate;
};
export var clsx = function clsx() {
  for (var _len = arguments.length, classes = new Array(_len), _key = 0; _key < _len; _key++) {
    classes[_key] = arguments[_key];
  }

  return classes.filter(Boolean).join(' ');
};